# cell-classifier-mlops
An imbalanced biomedical image classifier with a frozen ViT backbone. Trained reproducibly, versioned in a model registry, served behind an API, and shipped by CI/CD with a rollback path.
